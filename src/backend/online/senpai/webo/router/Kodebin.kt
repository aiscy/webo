package online.senpai.webo.router

import io.ktor.routing.*

fun Route.kodebin() {
    /*route("view") {
        get {
            val session = call.sessions.get<AuthenticatedUser>()
            if (session != null) {
                call.respondText(session.count.toString())
            } else {
                call.respondText("null")
            }
        }
    }*/
}
